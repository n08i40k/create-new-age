dependencies {
    neoForge("net.neoforged:neoforge:${project.neoforgeVersion}")

    modCompileOnly("com.simibubi.create:create-${rootProject.minecraftVersion}:${rootProject.createVersion}:slim") { transitive = false }
    modImplementation("net.createmod.ponder:ponder-neoforge:${ponderVersion}+mc${minecraftVersion}")
    modImplementation("dev.engine-room.flywheel:flywheel-neoforge-${minecraftVersion}:${flywheelVersion}")
    compileOnly("dev.engine-room.flywheel:flywheel-neoforge-api-${minecraftVersion}:${flywheelVersion}")
    modCompileOnly("com.tterrag.registrate:Registrate:${rootProject.registrateVersion}")

    modCompileOnly("cc.tweaked:cc-tweaked-${rootProject.minecraftVersion}-common-api:${rootProject.cctVersion}")
    if (enableCCT.toBoolean()) {
        // TODO: Make this platform agnostic
        modCompileOnly("cc.tweaked:cc-tweaked-${rootProject.minecraftVersion}-forge-api:${rootProject.cctVersion}")
    }

//    modCompileOnly("net.fabricmc:fabric-loader:${project(":fabric").fabricLoaderVersion}")
//    modCompileOnly("curse.maven:forge-config-api-port-fabric-547434:4583000")

    modCompileOnly("com.google.code.findbugs:findbugs:3.0.1")
    annotationProcessor(implementation("io.github.llamalad7:mixinextras-common:0.4.1"))

    api("dev.ryanhcode.sable-companion:sable-companion-common-${minecraftVersion}:${project.sableCompanionVersion}")
}
